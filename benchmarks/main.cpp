#include <iostream>

// Placeholder benchmark entry point. Stage 10's matmul GFLOPS harness lands
// here; for now this exists so the target builds and links against autograd.
int main() {
    std::cout << "no benchmarks yet" << std::endl;
    return 0;
}
